<style type="text/css">
 INPUT[type='text'] {
   width: 95%;
 }
</style>
<div class="window">
	<h2>Contact for &ldquo;<%= @address_book.name -%>&rdquo;</h2>
	<%= form_for([@address_book, @contact]) do |f| %>
	  <%= render 'form', :f => f %>
    <div>
      <%= f.submit "Update" -%> <%= link_to 'Back to contact', address_book_contact_url(@address_book, @contact) -%> <%= link_to 'Back to address book', address_book_url(@address_book) -%>
    </div>
  <% end -%>
</div>